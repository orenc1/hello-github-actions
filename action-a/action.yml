name: "Hello Actions"
description: "Greet someone"
author: "orenc@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Oren"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
